version: "3.8"

services:
  # web:
  #   build:
  #     context: "."
  #     dockerfile: "Dockerfile.standalone"
  #     network: container:meilisearch
  #     args:
  #       MEILISEARCH_URL: "meilisearch:7700"
  #   depends_on:
  #     meilisearch:
  #       condition: service_completed_successfully
  meilisearch:
    image: getmeili/meilisearch
    healthcheck:
      test: "curl -X GET 'http://localhost:7700/health' || exit 1"
      interval: 10s
      timeout: 10s
      retries: 3
